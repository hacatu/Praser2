CFLAGS += -Iinclude -Llib -g -Wall -Wno-switch -fPIC
LDFLAGS += -Llib

!cc = |> gcc $(CFLAGS) -c %f -o %o |>
!ld = |> gcc $(LDFLAGS) %f -o %o |>
!ar = |> ar rcs %o %f |>
!so = |> gcc -shared %f -o %o |>

